import styled from 'styled-components';
import { COLORS } from 'styles/constants';

export const Title = styled.h1`
  font-size: 4rem;

  color: transparent;
  -webkit-text-stroke: 1px ${COLORS.TEXT.STANDARD};
  text-align: center;
`;
